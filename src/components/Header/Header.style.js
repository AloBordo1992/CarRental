import styled from 'styled-components';

export const Nav = styled.ul`
  display: flex;
  align-items: flex-start;
  gap: 16px;
  font-size: 12px;
  font-style: normal;
  font-weight: 500;
  line-height: normal;
`;
